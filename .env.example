# Application Password for Authentication
APP_PASSWORD=123

# JWT Configuration
# Secret key for signing JWT tokens (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-secret-key-change-this-in-production

# JWT expiration time (examples: 1h, 24h, 7d, 30d)
JWT_EXPIRES_IN=24h

# Base Path Configuration (optional)
# If you want to deploy the app under a subpath (e.g., /car-tracker)
# Leave empty for root path deployment
# NEXT_PUBLIC_BASE_PATH=/car-tracker

# Database Configuration
# Path to the SQLite database file (relative to project root or absolute path)
# For Docker: /app/data/car-expense-tracker.db
# For local: car-expense-tracker.db
DB_PATH=car-expense-tracker.db
